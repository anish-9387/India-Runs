# Core ranking dependencies.
# PRIMARY BUILD SYSTEM: Poetry (see pyproject.toml).
# This file is kept for pip-only workflows.
#   pip install -r requirements.txt
#   python rank.py --candidates ...
numpy>=2.5.0
scikit-learn>=1.3
